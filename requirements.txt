# Requirements for FastDepth + CORES OOD Detection
# ==================================================
# Install with:  pip install -r requirements.txt
#
# For GPU support, install the appropriate PyTorch variant first:
#   https://pytorch.org/get-started/locally/
#   e.g. pip install torch torchvision --index-url https://download.pytorch.org/whl/cu121

torch>=2.0.0
torchvision>=0.15.0
numpy>=1.24.0
scikit-learn>=1.3.0
matplotlib>=3.7.0
